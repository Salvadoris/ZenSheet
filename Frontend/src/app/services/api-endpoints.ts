const baseApiEndpoint = "/api/";
const baseHubEndpoint = "/hubs/";

export const apiEndpoints = {
    Folder: `${baseApiEndpoint}Folder`,
    Note: `${baseApiEndpoint}Note`,
    Canvas: `${baseHubEndpoint}Canvas`,
    Health: `${baseApiEndpoint}Health`
}